<Window x:Class="Machine_Performance_Management.Chart.ChartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Machine_Performance_Management.Chart" 
        d:DataContext="{d:DesignInstance Type=local:ChartViewModel}"
        
        xmlns:lv="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Biểu đồ hiệu suất" Height="450" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">

            <!-- CheckBox -->
            <CheckBox Content="Loại máy"
              IsChecked="{Binding IsByMachineType}" />

            <!-- CheckBox -->
            <CheckBox Content="Máy"
                      Margin="30 0 0 0"
              IsChecked="{Binding IsByMachine}" />
            
            <TextBlock Text="Time"
                       Margin="30 0 0 0"/>
            <!-- ComboBox -->
            <ComboBox ItemsSource="{Binding AvailableDates}"
              SelectedItem="{Binding SelectedDate}"
              Width="150" Margin="10,0,0,0">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding StringFormat='{}{0:dd/MM/yyyy}'}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
                <ComboBox.ItemContainerStyle>
                    <Style TargetType="ComboBoxItem">
                        <Style.Triggers>
                            <!-- Nếu Display = ALL thì đổi sang chữ xanh dương -->
                            <DataTrigger Binding="{Binding Display}" Value="ALL">
                                <Setter Property="Foreground" Value="Blue"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ComboBox.ItemContainerStyle>
            </ComboBox>

        </StackPanel>
        
        <lv:CartesianChart Grid.Row="1"
                   Series="{Binding Series}"
                   LegendLocation="Right"
                   Padding="0"
                   Background="WhiteSmoke"
                   BorderBrush="Gray"
                   BorderThickness="1"
                   Margin="20"
                   FontFamily="Segoe UI">
            <lv:CartesianChart.AxisX>
                <lv:Axis Title="Nhóm" Labels="{Binding Labels}"
                     FontSize="16"
                     FontWeight="Bold"
                     Foreground="Black"/>
            </lv:CartesianChart.AxisX>

            <lv:CartesianChart.AxisY>
                <lv:Axis Title="Hiệu suất (%)"
                     LabelFormatter="{Binding YFormatter}"
                     FontSize="16"
                     FontWeight="Bold"
                     Foreground="Black"/>
            </lv:CartesianChart.AxisY>
        </lv:CartesianChart>
    </Grid>
</Window>
